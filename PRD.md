# 旅遊準備清單工具 — 產品需求書 (PRD)

| 項目 | 內容 |
|---|---|
| 版本 | v2.0 |
| 日期 | 2026-09-14 |
| 狀態 | 需求已確認，可交辦 |
| 目標平台 | 手機優先網頁 (PWA)，桌機可用 |
| 使用規模 | 自己與朋友（非公開產品） |

> **v2.0 相對 v1.1 的變更**：新增行程頁（§5.10），並以行程頁**取代**原本的「共同分頁」。連帶影響：專案起訖日期改為必填、`Item.owner_user_id` 改為必填、原 F-25（共同分頁標籤同名合併）刪除。變更清單見 §11。

---

## 1. 背景與目標

出國前在 IG、Google Maps、部落格看到想去的餐廳、想買的東西，散落在截圖、書籤、聊天紀錄裡，出發前找不到、到現場忘記買。這個工具把它們集中在一個地方：出發前貼連結就能存、用地區和標籤整理；把確定要去的排進行程；旅途中用手機打開就看得到，沒網路也能看、能勾「已買」。同行朋友各自整理自己的清單互相可看，行程則是全隊共用一份。

### 1.1 產品目標

1. **收藏快**：貼上連結 → 自動帶入店名與圖片 → 存檔，30 秒內完成。
2. **排程不痛**：只分早上 / 下午 / 晚上，不強迫填時間。只有訂位、車票這種有時間壓力的才填，填了就明顯標出來。
3. **現場好用**：手機單手操作、離線可讀、購買狀態與行程完成一鍵切換。
4. **各自整理，共同出發**：每人有自己的清單分頁互相可看不可改；行程是全隊共用一份，誰都能改。

### 1.2 非目標（v1 不做）

- **小時刻度的時間軸**。行程只有早 / 中 / 晚三個時段，不畫 08:00–09:00 的格子。
- 地圖總覽、路線規劃、交通時間自動估算
- 自動行程建議、景點推薦
- 花費統計、分帳
- 原生 App 上架（iOS / Android）
- 公開分享連結給非成員
- 即時多人協同編輯（Google Docs 式同步）
- 一個專案跨多個國家
- 行程提醒通知

---

## 2. 名詞定義

| 名詞 | 定義 |
|---|---|
| **專案 (Trip)** | 一趟旅程，對應**一個國家**，有明確起訖日期。例：「2026 秋・日本」 |
| **地區 (Region)** | 專案內的地理分類，使用者自訂。例：東京、大阪、京都 |
| **清單分頁 (List Tab)** | 每位成員一個，放自己想去的地點與想買的東西。**他人唯讀** |
| **行程 (Itinerary)** | 專案內唯一一份、**全隊共用**的日程表。取代舊版的「共同分頁」 |
| **行程日 (Day)** | 行程中的一天，由專案起訖日期自動產生 |
| **時段 (Slot)** | 一天分早上 / 下午 / 晚上三段 |
| **餐食備選 (Meal Candidate)** | 一天的早餐 / 午餐 / 點心 / 晚餐 / 宵夜，每一餐可填多個候選 |
| **項目 (Item)** | 清單裡的一筆資料，分兩種類型：**地點**、**購物** |
| **行程項目 (Entry)** | 行程裡的一筆資料。可以**引用**清單裡的地點，也可以自由輸入 |
| **標籤 (Tag)** | 使用者自訂的分類關鍵字，**僅屬於自己、僅限該專案內** |
| **擁有者 (Owner)** | 建立專案的人，每個專案恰有一位 |
| **成員 (Member)** | 透過邀請加入專案的人 |

---

## 3. 使用者與權限

### 3.1 角色

v1 只有兩種角色：**擁有者**、**成員**。不區分「編輯者 / 檢視者」，被邀請進來就是成員。

### 3.2 權限矩陣

| 動作 | 擁有者 | 成員 |
|---|:-:|:-:|
| 編輯專案名稱 / 國家 / 封面 / 日期 | ✔ | ✘ |
| 刪除專案 | ✔ | ✘ |
| 新增 / 改名 / 刪除 / 排序地區 | ✔ | ✔ |
| 產生邀請連結 | ✔ | ✔ |
| 撤銷邀請連結 | ✔ | 僅自己產生的 |
| 移除成員 | ✔ | ✘ |
| 自行離開專案 | ✘（需先刪除專案） | ✔ |
| **自己的清單分頁**：新增 / 編輯 / 刪除項目 | ✔ | ✔ |
| **他人的清單分頁** | 唯讀 | 唯讀 |
| **行程**：新增 / 編輯 / 刪除任何行程項目 | ✔ | ✔ |
| **行程**：勾選完成、編輯每日備註 | ✔ | ✔ |
| 複製他人項目到自己的清單分頁 | ✔ | ✔ |
| 把任何看得到的地點加入行程 | ✔ | ✔ |
| 自己的標籤：新增 / 改名 / 刪除 | ✔ | ✔ |

> 行程是全隊共用一份，刻意不設個人隔離。理由：行程的價值在於大家看同一份。個人的想法放在自己的清單分頁。

### 3.3 成員離開或被移除

- 該成員的清單分頁**保留、變唯讀**，分頁名稱標示「（已離開）」。
- 擁有者可以刪除已離開成員的分頁（含其中所有項目）。
- 該成員排進**行程**的項目**保留**，不受影響。若行程項目引用的是該成員的地點，引用關係保留且可讀。
- 若同一人之後再被邀請回來，恢復原分頁的編輯權。

---

## 4. 資料模型

> 給工程師的邏輯模型，欄位型別為建議。「清單分頁」不需獨立資料表：`Item.owner_user_id` 即為分頁歸屬。「行程日」不需獨立資料表：由專案起訖日期推導，`TripDay` 只在使用者寫每日備註時才產生資料列。

### 4.1 實體

**User**
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | uuid | PK |
| google_sub | string | Google 帳號唯一識別，unique |
| email | string | |
| display_name | string | 來自 Google，使用者可改 |
| avatar_url | string | 來自 Google |
| created_at | timestamp | |

**Trip（專案）**
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | uuid | PK |
| name | string(50) | 必填 |
| country_code | string(2) | ISO 3166-1 alpha-2，必填 |
| cover_image_key | string | 選填 |
| start_date / end_date | date | **必填**（行程日由此產生）；end ≥ start；最長 60 天 |
| owner_id | uuid → User | |
| deleted_at | timestamp | 軟刪除 |
| created_at / updated_at | timestamp | |

**TripMember**
| 欄位 | 型別 | 說明 |
|---|---|---|
| trip_id | uuid → Trip | 複合 PK |
| user_id | uuid → User | 複合 PK |
| role | enum: owner / member | |
| status | enum: active / left | |
| joined_at / left_at | timestamp | |

**Invite（邀請連結）**
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | uuid | PK |
| trip_id | uuid → Trip | |
| token | string | 隨機不可猜（≥ 32 bytes），unique |
| created_by | uuid → User | |
| expires_at | timestamp | 建立後 7 天 |
| revoked_at | timestamp | 撤銷時間，null = 有效 |

**Region（地區）**
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | uuid | PK |
| trip_id | uuid → Trip | |
| name | string(30) | 專案內 unique |
| sort_order | int | |

**Item（項目：清單裡的地點或購物）**
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | uuid | PK |
| trip_id | uuid → Trip | |
| owner_user_id | uuid → User | **必填**。項目屬於哪個人的清單分頁 |
| type | enum: place / shopping | 建立後不可改 |
| title | string(100) | 必填 |
| region_id | uuid → Region, nullable | 兩種類型都可設，null = 未分類 |
| url | string(2048) | 選填 |
| url_title | string | 連結預覽抓到的標題（原始值，供還原） |
| url_image_key | string | 連結預覽圖轉存後的 key |
| note | text(2000) | 備註，多行 |
| visited | boolean | **僅 place**，已去過，預設 false |
| purchase_status | enum: todo / bought / not_found | **僅 shopping**，預設 todo |
| planned_store | string(100) | **僅 shopping**，預計購買地點（純文字） |
| sort_order | int | 手動排序用，v1 依 created_at 即可 |
| created_at / updated_at | timestamp | updated_at 用於離線衝突判定 |

**ItemImage**
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | uuid | PK |
| item_id | uuid → Item | |
| storage_key | string | 轉存到自己儲存空間的 key |
| source_url | string | 若原本是貼網址，保留來源 |
| width / height | int | 供版面預留高度 |
| sort_order | int | |

**Tag（標籤）**
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | uuid | PK |
| trip_id | uuid → Trip | 標籤限專案內 |
| user_id | uuid → User | 標籤屬於個人 |
| name | string(20) | (trip_id, user_id, name) unique |

**ItemTag**
| 欄位 | 說明 |
|---|---|
| item_id → Item, tag_id → Tag | 複合 PK |

**ItineraryEntry（行程項目）** — 新增
| 欄位 | 型別 | 說明 |
|---|---|---|
| id | uuid | PK |
| trip_id | uuid → Trip | |
| date | date | 哪一天 |
| section | enum: schedule / meal | 上方行程區 / 下方餐食備選區 |
| slot | enum | `section=schedule` → morning / afternoon / evening；`section=meal` → breakfast / lunch / snack / dinner / late_night。以 CHECK 限制組合合法 |
| kind | enum: place / transport | `section=meal` 時恆為 place |
| item_id | uuid → Item, nullable | **引用**清單項目。null = 自由輸入 |
| title | string(100) | `item_id` 為 null 時必填；有引用時顯示引用項目的標題，此欄留空 |
| transport_mode | string(30) | 僅 `kind=transport`。例：「JR 橫須賀線」「步行」「計程車」 |
| start_time / end_time | time, nullable | 有值即視為「有時間」並特別標註（見 F-40） |
| note | text(500) | **當天備註**，只屬於這一筆行程，不寫回被引用的項目 |
| done | boolean | 已完成，預設 false，全隊共用 |
| sort_order | int | 同一 (date, section, slot) 內的順序 |
| created_by / updated_by | uuid → User | 顯示「由誰排的」 |
| created_at / updated_at | timestamp | |

索引建議：`(trip_id, date, section, slot, sort_order)`。

**TripDay（每日備註）** — 新增
| 欄位 | 型別 | 說明 |
|---|---|---|
| trip_id | uuid → Trip | 複合 PK |
| date | date | 複合 PK |
| note | text(500) | 這天的備註。例：住哪間飯店、天氣、備案 |
| updated_by | uuid → User | |
| updated_at | timestamp | |

> 只在使用者實際寫備註時才 insert。沒有備註的那一天不需要資料列。

### 4.2 關鍵規則

**標籤**
- 一個項目只能貼**貼標籤者自己的**標籤（`Tag.user_id` = 目前使用者）。因為項目一定屬於某一個人的分頁，不會出現一個項目被多人貼標籤的情況。
- 標籤不作用在行程項目上。行程項目用「當天備註」。

**引用（行程 → 清單項目）**
- `ItineraryEntry.item_id` 是**引用不是複製**。被引用項目的標題、圖片、連結、備註改了，行程裡跟著變。
- 引用**他人**的地點：行程裡點進去看到的是唯讀的項目內容，但該筆行程的「當天備註」「時間」「完成」任何成員都能改。
- **完成勾選的連動**：勾選行程項目的「完成」時，若它引用的是**勾選者自己的**地點，同時把該地點的 `visited` 設為 true；若引用的是他人的地點，只更新行程的 `done`，不動對方的資料（沒有權限）。取消勾選同理。
- 只有 `type = place` 的項目可以被行程引用。購物項目不進行程。

**刪除的連鎖**
- 刪除 Region → 該地區下項目的 `region_id` 設為 null（未分類），不刪項目。
- 刪除 Tag → 移除所有 ItemTag 關聯，不刪項目。
- 刪除 Item → 連同 ItemImage 與儲存空間檔案一起刪；若該項目被行程引用，見 F-47。
- 刪除 ItineraryEntry → 不影響被引用的 Item。
- 刪除 Trip → 連同 ItineraryEntry、TripDay 一併軟刪除。

---

## 5. 功能需求

優先級：**P0** = v1 必做；**P1** = v1 有餘裕就做；**P2** = 之後版本。
每項附驗收條件 (AC)。

### 5.1 帳號與登入

**F-01 Google 登入** — P0
- 使用 Google OAuth 2.0 / OpenID Connect 登入，首次登入自動建立 User。
- 提供登出。
- AC：未登入直接打開任一專案網址 → 導向登入頁 → 登入後回到原本要開的網址。
- AC：登入狀態至少保留 30 天，不需每次重新登入。

### 5.2 專案管理

**F-02 專案列表** — P0
- 顯示「我擁有」與「我參與」的所有專案，卡片含：封面（無則顯示國旗）、名稱、國家、成員頭像（最多 4 個 + N）、起迄日期。
- 排序：進行中（今天落在起訖日內）的排最前，其次依 `start_date` 由近到遠，已結束的排最後。
- 空狀態：提示「建立第一個旅程」。

**F-03 建立專案** — P0
- 欄位：名稱（必填，≤ 50 字）、國家（必填，從 ISO 國家清單可搜尋選擇，顯示國旗 + 中文名）、**起訖日期（必填）**、封面圖（選填）。
- 起訖日期必填的原因：行程的每一天由此產生。`end_date ≥ start_date`，最長 60 天。
- 建立者自動成為擁有者，自動擁有清單分頁；行程自動存在並依日期產生空的每一天。
- AC：建立後直接進入專案頁的行程分頁，看到 Day 1 到 Day N 的空框架。

**F-04 編輯 / 刪除專案** — P0
- 僅擁有者。刪除需二次確認（顯示專案名、項目數量、行程項目數量）。
- 刪除為軟刪除（`deleted_at`），UI 不提供復原；保留 30 天後由排程清除。
- 改日期的處理見 F-46。

**F-05 地區管理** — P0
- 所有成員可新增 / 改名 / 刪除 / 拖曳排序。名稱專案內唯一，≤ 30 字。
- 刪除地區時提示「N 個項目將變為未分類」，確認後執行。
- AC：地區改名後，所有使用該地區的項目立即顯示新名稱。

**F-06 邀請成員** — P0
- 任何成員可產生邀請連結，格式 `https://<domain>/invite/<token>`，7 天有效，可撤銷。
- 提供「複製連結」與「分享」（呼叫 Web Share API，讓使用者丟到 LINE 等）。
- 被邀請者開連結：未登入 → 先 Google 登入 → 顯示「{邀請人} 邀請你加入『{專案名}』」與成員頭像 → 點「加入」→ 成為成員並進入專案。
- 已是成員點連結 → 直接進專案。
- 連結過期或已撤銷 → 顯示「邀請已失效，請請對方重新產生」。
- AC：同一連結可被多人使用，直到過期或撤銷。

**F-07 成員管理** — P0
- 成員列表：頭像、名稱、角色、加入時間。
- 擁有者可移除成員；成員可自行離開。行為見 §3.3。
- AC：被移除者下次載入時看不到該專案，其分頁對其他人變唯讀並標示「已離開」。

### 5.3 分頁

**F-09 分頁列** — P0
- 專案頁頂端水平可捲動的分頁列：**［行程］［我的］［成員 A］［成員 B］…**
- 「行程」固定第一個，圖示與其他分頁區隔（行程是日程表，其他是清單）。
- 成員分頁以頭像 + 名稱顯示；已離開者以灰色 + 「（已離開）」標示，排最後。
- 預設打開「行程」；記住使用者上次停留的分頁（本機）。

**F-10 清單分頁內結構** — P0
- 清單分頁內有兩個子清單，以 segment control 切換：**地點** | **購物**。
- 切換分頁時保留目前的子清單選擇（例：在「我的・購物」切到「成員 A」→ 顯示「成員 A・購物」）。
- 行程分頁沒有這個 segment control，結構見 F-37。

**F-11 他人分頁唯讀** — P0
- 隱藏所有新增 / 編輯 / 刪除入口、購買狀態與已去過切換。
- 每個項目提供兩個動作：「複製到我的分頁」（F-12）、「加入行程」（F-41）。
- AC：透過 API 直接對他人分頁寫入必須被拒絕（403），前端隱藏不算完成。

**F-12 複製項目到自己的分頁** — P0
- 複製：標題、連結與預覽、圖片（引用同一份檔案，不重複上傳）、備註、地區、預計購買地點、類型。
- 標籤：以**名稱**對應到自己在此專案的標籤，不存在則自動建立。
- 購買狀態重設為「未買」，已去過重設為 false。
- 複製後兩者獨立，互不同步。
- 註：要放進行程**不需要**先複製，直接用 F-41 引用即可。
- AC：複製後停留原分頁，顯示 toast「已複製到我的分頁」並可點擊前往。

### 5.4 地點項目

**F-13 新增 / 編輯地點** — P0
- 欄位：標題（必填）、連結（選填）、地區（選填，下拉，可在此直接新增地區）、標籤（多選，可直接輸入新增）、圖片（≤ 5 張）、備註（多行，≤ 2000 字）。
- 表單為單頁滾動，儲存按鈕固定底部。

**F-14 貼連結自動預覽** — P0
- 連結欄位貼上或失焦後，前端呼叫後端預覽 API，後端抓取目標頁面的 Open Graph（`og:title`、`og:image`、`og:description`）與 `<title>`。
- 需跟隨轉址（Google Maps 短網址 `maps.app.goo.gl`、`goo.gl/maps`）。
- 成功：標題欄空白則填入 `og:title`；預覽圖下載轉存到自己的儲存空間並顯示；描述**不**自動填入備註，但顯示在預覽卡上可一鍵「加入備註」。
- 失敗或超時（5 秒）：顯示「無法取得預覽，請手動填寫」，仍可儲存。
- 使用者可清除預覽圖、改標題；重新貼不同連結會再抓一次並詢問是否覆蓋。
- **安全**：後端只允許 `http` / `https`，解析 DNS 後拒絕私有網段（127.0.0.0/8、10/8、172.16/12、192.168/16、169.254/16、::1 等），限制回應大小 2 MB、逾時 5 秒，防 SSRF。
- AC：貼 `https://maps.app.goo.gl/xxxx` → 3 秒內帶入店名與圖片。
- AC：貼 `http://192.168.1.1` → 後端拒絕，前端顯示無法預覽。

**F-15 地點卡片** — P0
- 顯示：縮圖（第一張圖片，無則預覽圖，無則 placeholder）、標題、地區 chip、標籤 chips、備註前兩行、「開啟連結」按鈕。
- 已排進行程的地點，卡片上顯示一個小徽章「10/12 下午」，點擊跳到行程該位置。
- 「開啟連結」以新分頁開啟原始網址（手機上 Google Maps 連結會跳到地圖 App）。
- 點卡片進入詳細頁（見 P-06）。

**F-16 依地區分組與篩選** — P0
- 篩選列：地區 chips ［全部］［東京］［大阪］…［未分類］，單選。
- 選「全部」時清單**依地區分組**顯示區段標題與數量；選特定地區則平鋪。
- 地區 chip 順序依 Region.sort_order。

**F-17 刪除地點** — P0
- 詳細頁與長按卡片皆可刪除，需確認。被行程引用時的處理見 F-47。

**F-36 地點「已去過」勾選** — P1
- 卡片右側有勾選鈕，一鍵切換已去過 / 未去，不需進編輯頁；行為與 F-19 相同（樂觀更新、離線可用）。
- 已去過：卡片淡化 + 打勾標示；在地區分組內排到該區段最後。
- 篩選列（地點）增加狀態 chips ［全部］［未去］［已去過］，單選，與地區、標籤 AND。
- 地點子清單頂端顯示「已去 3 / 12」（與 F-21 同一元件）。
- 與行程完成勾選的連動見 §4.2。

### 5.5 購物項目

**F-18 新增 / 編輯購物** — P0
- 欄位：標題（必填）、連結、圖片（≤ 5）、標籤、地區（選填）、**預計購買地點**（純文字，≤ 100 字，例：「唐吉訶德 道頓堀店」）、**購買狀態**（未買 / 已買 / 沒買到，預設未買）、備註。
- 連結預覽行為同 F-14。
- 購物項目不進行程。

**F-19 一鍵切換購買狀態** — P0
- 卡片右側有狀態按鈕，點擊彈出三個選項（未買 / 已買 / 沒買到），不需進編輯頁。
- 切換立即反映在畫面（樂觀更新），離線時排入同步佇列（見 F-33）。
- 已買：卡片淡化 + 標題刪除線；沒買到：灰色 + 標示「沒買到」。

**F-20 狀態篩選與排序** — P0
- 篩選列：狀態 chips ［全部］［未買］［已買］［沒買到］，單選，可與地區、標籤篩選同時生效（AND）。
- 預設「全部」，排序：未買 → 沒買到 → 已買；同狀態內依建立時間新到舊。

**F-21 購買進度** — P1
- 購物子清單頂端顯示「已買 3 / 12」與進度條（沒買到不計入已買）。

### 5.6 標籤與篩選

**F-22 標籤歸屬** — P0
- 標籤屬於「使用者 × 專案」，不跨專案、不與其他成員共用。
- 每人每專案最多 50 個標籤，名稱 ≤ 20 字。
- 顏色由系統依名稱雜湊自動配色（固定 8 色），使用者不需選色。

**F-23 標籤管理** — P0
- 在項目編輯時輸入即建立（輸入文字 → 顯示「建立『拉麵』」）。
- 標籤管理頁：改名、刪除、顯示使用數。刪除時提示「N 個項目將移除此標籤」。
- AC：改名後所有項目即時反映。

**F-24 篩選機制** — P0
- 每個子清單上方有一列篩選 chips，分三組：地區、狀態（購物 = 購買狀態；地點 = 已去過）、標籤。每組**單選**，三組間為 AND。
- 選中的篩選條件記住在本機，切換分頁不重設。
- 篩選結果為空顯示「沒有符合的項目」+「清除篩選」。

**F-26 關鍵字搜尋** — P1
- 子清單頂端搜尋框，比對標題、備註、預計購買地點，即時過濾（前端），與篩選 AND。

> F-25（共同分頁標籤同名合併）隨共同分頁移除而刪除。

### 5.7 圖片

**F-27 上傳圖片** — P0
- 使用 `<input type="file" accept="image/*" multiple>`；手機上可選「拍照」或「相簿」。
- 前端壓縮：長邊縮至 1600 px、輸出 JPEG 品質 0.8；壓縮後上限 2 MB。原檔上限 20 MB，超過提示。
- 每項目最多 5 張，超過時提示。
- 上傳中顯示進度與可取消；失敗可重試。
- 註：iOS 的 HEIC 由 Safari 在選檔時自動轉為 JPEG，不需額外處理；Android 若遇 HEIC 由前端 canvas 轉檔失敗則提示不支援。

**F-28 貼圖片網址** — P0
- 提供「貼圖片網址」入口。後端下載該圖片並轉存到自己的儲存空間（同 F-14 預覽圖機制，含 SSRF 防護與大小限制 10 MB）。
- 下載失敗（403、非圖片、超時）→ 提示「無法取得圖片」，不儲存。
- 理由：IG、Google 圖片網址會過期，且離線需要有自己的副本。

**F-29 圖片檢視** — P1
- 點縮圖全螢幕檢視，左右滑動切換，雙指縮放（用瀏覽器原生）。

### 5.8 備註

**F-30 備註** — P0
- 多行純文字，≤ 2000 字，保留換行。
- 顯示時自動把網址轉為可點連結。
- 卡片顯示前兩行，詳細頁顯示全部。
- 行程項目的「當天備註」上限 500 字，其餘行為相同。

### 5.9 離線 (PWA)

**F-31 可安裝** — P0
- 提供 Web App Manifest（名稱、icons 192/512、`display: standalone`、主題色）與 Service Worker。
- 第 2 次開啟後提示「加入主畫面」（iOS 顯示手動步驟說明）。

**F-32 離線可讀** — P0
- 使用者**曾在線上開過**的專案，其所有清單分頁、**行程與餐食備選**、每日備註與縮圖快取到本機（IndexedDB + Cache Storage）。
- 離線時開啟：顯示快取資料，頂端顯示「離線模式・資料為 {時間} 版本」。
- 未曾開過的專案離線時顯示「需要網路才能載入」。
- 圖片全尺寸檔不強制快取，離線點開顯示縮圖即可。
- **行程頁是離線優先級最高的畫面**：旅途中沒網路要能看到今天排了什麼、幾點的車、訂位時間。

**F-33 離線可勾選** — P0
- 離線時**僅**允許三種寫入：切換購買狀態、切換地點已去過、切換行程項目完成。其他寫入操作按鈕停用並提示「需要網路」。
- 變更寫入本機佇列，畫面即時反映；恢復連線後自動依序送出，成功後清除。
- 頂端顯示「待同步 N 筆」；同步失敗（如項目已被刪除）顯示提示並丟棄該筆。

**F-34 衝突處理** — P1
- Last-write-wins：以 `updated_at` 較新者為準，不做欄位合併。
- 兩人同時改同一筆行程項目，後存的覆蓋先存的，不另提示。

**F-35 資料更新** — P0
- 線上時每次進入專案頁自動拉最新資料；提供下拉刷新。
- v1 不做即時推播；他人修改需重新整理才看到。

### 5.10 行程（新增）

**F-37 行程頁結構** — P0
- 行程分頁由上而下：**日期選擇列 → 當天內容**。
- **日期選擇列**：水平可捲動，每個 chip 顯示「10/12（五）Day 3」。今天所在的那天加重標示並在進頁時自動捲到該位置；若今天不在旅程期間內，預設落在 Day 1。
- **當天內容**分兩個區塊，上下排列，互不干涉：

  **上半：行程區**（`section = schedule`）
  - 三個時段區段：**早上 / 下午 / 晚上**，永遠三個都顯示，空的顯示「＋ 加入行程」。
  - 每個時段內是一列項目卡，可拖曳排序。
  - 沒有小時刻度、沒有時間軸格線。

  **下半：餐食備選區**（`section = meal`）
  - 五個列：**早餐 / 午餐 / 點心 / 晚餐 / 宵夜**，每列可放多個候選。
  - 預設只展開有內容的列；空的列收合成一排小按鈕「＋ 早餐 ＋ 午餐 ＋ 點心 ＋ 晚餐 ＋ 宵夜」。
  - 餐食區是「這天可以吃的口袋名單」，不佔行程位置。同一間店也可以另外排進行程區，兩者不互相影響。

- 頁面頂端顯示當天摘要：「行程 5 項・餐食備選 7 間・已完成 3」。
- AC：一天完全沒有任何內容時，行程區仍顯示三個空時段，餐食區顯示五個收合按鈕，不顯示空白畫面。

**F-38 新增行程項目** — P0
- 每個時段的「＋」提供兩個入口：
  1. **從清單選**（F-41）
  2. **自己打**：只填標題就能存
- 行程項目欄位：標題（引用時自動帶入且不可改，自由輸入時必填）、類型（一般 / 交通）、開始時間（選填）、結束時間（選填）、交通方式（僅交通類型）、當天備註（≤ 500 字）。
- 表單為底部彈出面板，不換頁，填完即存。
- AC：從按下「＋」到存好一筆只打標題的項目 ≤ 3 次點擊。

**F-39 交通項目** — P0
- 類型選「交通」時，卡片改用不同樣式（左側直線 + 交通圖示），與一般行程項目在視覺上明顯區隔。
- 專屬欄位：**交通方式**（純文字 ≤ 30 字，例「JR 橫須賀線」「步行」「巴士 100 號」）。提供常用選項快選：步行 / 電車 / 巴士 / 計程車 / 開車 / 飛機 / 船，選了可再改字。
- 標題建議格式「新宿 → 鎌倉」，但不強制，使用者自由打。
- 交通項目可以貼連結（車票訂單、時刻表）與填時間，與一般項目相同。
- 交通項目不引用清單地點（`item_id` 恆為 null）。

**F-40 時間標註** — P0
- 有填 `start_time` 的項目：卡片左側顯示時間（`14:30`，等寬數字），並加一個明顯的時間徽章與色塊，與沒填時間的項目一眼看得出差別。
- 有填 `end_time` 的另外顯示「14:30 – 16:00」。
- 同一時段內排序：**有時間的排在前面且依時間由早到晚；沒時間的排在後面依 `sort_order`**。
- 有時間的項目被拖曳到沒時間的項目後面時，儲存排序但顯示順序仍依上述規則，並提示一次「有時間的項目會自動排在前面」。
- 只做視覺標註，**不發任何提醒通知**。
- AC：同一時段內有「10:00 早餐店」「訂位 12:00」「（無時間）逛街」三筆，顯示順序為 10:00 → 12:00 → 逛街。

**F-41 從清單加入行程（引用）** — P0
- 入口有兩個：行程時段的「＋ 從清單選」、清單地點卡片與詳細頁的「加入行程」。
- **從行程進入**：彈出選擇面板，列出**所有成員**清單分頁裡的地點（購物項目不出現），可依地區、標籤、成員篩選與搜尋；已排進這天的顯示已加入標記。可多選一次加入多筆。
- **從清單進入**：彈出選擇面板，選日期 → 選時段（早 / 中 / 晚）或餐別（早餐 / 午餐 / 點心 / 晚餐 / 宵夜）。
- 加入後建立 `ItineraryEntry` 且 `item_id` 指向該項目，**是引用不是複製**：
  - 改地點的標題、照片、連結、備註，行程裡同步變。
  - 行程裡可另外寫「當天備註」，只屬於這一筆，不寫回地點。
  - 引用他人的地點：內容唯讀，但時間、當天備註、完成勾選任何成員都能改。
- 同一個地點可以被排進多天或多個時段（例如去兩次），不阻擋，但加入時提示「這個地點已排在 10/12 下午」。
- AC：改了清單裡的照片，行程卡片的縮圖跟著換，不需要重新加入。

**F-42 餐食備選區** — P0
- 五個餐別各自獨立，一個餐別可放多筆候選，不限數量（建議上限 10 筆，超過提示）。
- 每筆候選可以是引用的地點或自由輸入的名字，欄位與行程項目相同但**沒有交通類型**。
- 候選之間可拖曳排序，代表偏好順序。
- 候選也能填時間（訂位用），標註方式同 F-40。
- AC：午餐放三間拉麵店候選，在店裡勾了其中一間完成，另外兩間維持未完成不消失。

**F-43 每日備註** — P0
- 每一天的頂端有一塊備註區，點擊展開輸入，≤ 500 字，任何成員可編輯。
- 用途是住宿、天氣、下雨備案、當天的注意事項。
- 空的時候顯示一行淡色「＋ 這天的備註」，不佔版面。

**F-44 完成勾選與排序** — P0
- 每個行程項目與餐食候選右側有完成勾選鈕，一鍵切換，全隊共用狀態。
- 已完成：卡片淡化 + 打勾，**位置不動**（行程是時間順序，完成的往下丟會讓人看不懂已經走到哪）。
- 連動地點 `visited` 的規則見 §4.2。
- 拖曳排序：時段內可調順序，也可跨時段、跨日期拖曳搬移（拖到日期列的某個日期 chip 上放開＝搬到那天）。
- 離線時可勾選完成，不可拖曳排序。

**F-45 行程離線** — P0
- 行程頁與餐食備選必須離線可讀，行為見 F-32。
- 離線時可勾選完成（F-33），其他編輯停用。
- 離線時日期選擇列、時段捲動、當天備註閱讀全部正常運作。

**F-46 專案日期變更** — P0
- 擁有者改專案起訖日期時：
  - **延長**：新的日子直接出現在日期列，內容空的。
  - **縮短**：不刪任何資料。落在新範圍外的日期，其項目仍然存在，在日期列**最後面**以警示色顯示「10/18（範圍外）」，並在該日頂端提示「這天已不在旅程日期內，請搬到其他天或刪除」。
- 存檔前顯示確認：「縮短後有 3 天、共 8 個行程項目會落在範圍外」。
- AC：把 5 天改成 3 天，第 4、5 天的項目一筆都沒消失，仍可拖回前三天。

**F-47 刪除被行程引用的清單項目** — P0
- 刪除一個已被行程引用的地點時，確認對話框額外顯示「此地點已排在 10/12 下午、10/14 午餐」。
- 確認刪除後：**行程項目保留**，`item_id` 設為 null，`title` 填入原本的標題，卡片改為純文字樣式並顯示一行「原項目已刪除」。時間、當天備註、完成狀態全部保留。
- 不連鎖刪除行程項目。理由：行程是旅途中要看的東西，不該因為別人整理清單而少一筆。
- AC：A 刪掉自己的拉麵店，B 打開行程仍看得到「一蘭拉麵 19:00」與當天備註，只是點不進詳細頁。

---

## 6. 頁面清單與主要流程

### 6.1 頁面

| 編號 | 頁面 | 說明 |
|---|---|---|
| P-01 | 登入頁 | 產品名 + 一句說明 + 「使用 Google 登入」 |
| P-02 | 專案列表 | F-02；右下 FAB「＋ 新旅程」 |
| P-03 | 建立 / 編輯專案 | F-03、F-04、F-46；編輯模式多「成員」「地區」「刪除專案」入口 |
| P-04 | 專案頁・行程分頁 | F-37 ~ F-46。日期列 → 每日備註 → 行程區（早/中/晚）→ 餐食備選區 |
| P-05 | 專案頁・清單分頁 | F-09 ~ F-12。地點/購物 segment → 篩選列 → 清單；右下 FAB「＋」（僅自己的分頁） |
| P-06 | 清單項目編輯 | F-13 / F-18，地點與購物共用一個表單，依類型顯示欄位 |
| P-07 | 清單項目詳細 | 全部圖片（橫滑）、標題、連結按鈕、地區、標籤、預計購買地點、狀態、完整備註、建立時間、已排入的行程徽章；動作：編輯、刪除、複製到我的分頁、加入行程 |
| P-08 | 行程項目編輯 | F-38、F-39。底部彈出面板：標題、類型、時間、交通方式、當天備註 |
| P-09 | 從清單選地點 | F-41。可篩選、搜尋、多選 |
| P-10 | 成員與邀請 | F-06、F-07 |
| P-11 | 地區管理 | F-05 |
| P-12 | 標籤管理 | F-23 |
| P-13 | 接受邀請 | F-06 |

### 6.2 流程 A：出發前收藏一間店

1. IG 看到餐廳 → 打開 Google Maps 找到店 → 複製分享連結
2. 開 App → 專案「日本」→「我的」分頁 → 地點 → ＋
3. 貼連結到「連結」欄 → 自動帶入店名與圖片（F-14）
4. 選地區「東京」、標籤「拉麵」→ 儲存
5. 回到清單，看到新卡片在「東京」區段

**目標**：從步驟 2 到 5 ≤ 30 秒、≤ 6 次點擊。

### 6.3 流程 B：出發前一週排行程

1. 專案「日本」→「行程」分頁 → 日期列點 10/12（Day 3）
2. 「下午」時段點「＋ 從清單選」→ 面板篩選地區「東京」→ 勾選清水寺、二年坂 → 加入
3. 清水寺卡片點進去 → 填時間 14:30、當天備註「預約導覽，提早 15 分鐘到」→ 存
4. 「下午」再點「＋ 自己打」→ 類型選「交通」→ 標題「京都站 → 清水寺」→ 交通方式選「巴士」→ 時間 14:00 → 存
5. 拉到下方餐食區 → 點「＋ 晚餐」→ 從清單選三間候選拉麵店
6. 這天頂端寫備註「住京都站前 APA」

**結果**：下午時段顯示「14:00 巴士 京都站 → 清水寺」「14:30 清水寺」，兩筆都有時間徽章，依時間排序。

### 6.4 流程 C：旅途中在店裡勾購物

1. 在唐吉訶德，手機沒網路 → 開 App（主畫面圖示）
2. 專案「日本」→「我的」→ 購物 → 篩選「未買」
3. 找到「合利他命」→ 點狀態 → 已買（畫面立刻淡化）
4. 回飯店連上 Wi-Fi → 自動同步 → 「待同步」消失

### 6.5 流程 D：旅途中看今天的行程

1. 早上出門前打開 App（沒網路）→ 落在「行程」分頁，自動捲到今天
2. 看到早上兩項、下午三項，其中「14:30 清水寺」有時間徽章，「14:00 巴士」也有
3. 中午吃完拉麵 → 在餐食區的午餐候選勾了「一蘭」→ 另外兩間維持未勾
4. 因為那間拉麵是自己存的，回到「我的」清單一看，它已經自動標成已去過

### 6.6 流程 E：邀請朋友

1. 擁有者 → 專案 → 成員 → 「產生邀請連結」→ 分享到 LINE
2. 朋友點連結 → Google 登入 → 看到「Jean 邀請你加入『2026 秋・日本』」→ 加入
3. 進入專案落在「行程」分頁，看到 Jean 已經排好的三天
4. 切到「Jean」分頁看她存的餐廳 → 對某一間點「加入行程」→ 選 10/13 晚餐

---

## 7. 非功能需求

| 類別 | 需求 |
|---|---|
| 裝置 | 手機優先，最小寬 360 px；桌機單欄置中（max-width 720 px）即可 |
| 瀏覽器 | iOS Safari 16+、Android Chrome 最新兩版、桌機 Chrome / Edge / Safari 最新兩版 |
| 效能 | 4G 下首屏可互動 < 3 秒；單一分頁 300 個項目、單一專案 60 天 × 每天 20 筆行程捲動不卡 |
| 連結預覽 | 伺服器端執行；逾時 5 秒；防 SSRF（見 F-14）；限制 2 MB |
| 圖片儲存 | 私有 bucket，以短效簽名網址存取，或不可猜的 key + 成員驗證；每張最多 2 MB |
| 授權 | **所有** API 檢查呼叫者為該專案 active 成員；寫入清單項目檢查 `owner_user_id` = 呼叫者；寫入行程只檢查成員身分；寫入專案設定檢查 owner。前端隱藏按鈕不算授權 |
| 時區 | 行程日期與時間一律視為**當地時間的純日期 / 純時間**，不做時區換算。`date` 存 DATE、`time` 存 TIME，不存 timestamp |
| 資料保護 | 每日備份；軟刪除的專案保留 30 天 |
| 語言 | 繁體中文，v1 單語，文案集中管理便於之後加英文 |
| 隱私 | 只存 Google 回傳的 email、名稱、頭像；不做追蹤分析 |
| 錯誤處理 | 所有寫入失敗要有可見提示與重試，不可靜默丟失 |

> 時區這條很重要。若用 timestamp 存「10/12 14:30」，人在台灣排、到日本看，會變成 15:30。務必存純日期與純時間。

---

## 8. 技術建議（參考，非強制）

目標是最少的自建後端。權限模型（成員 / 分頁擁有者 / 專案擁有者）可以直接用資料庫 Row-Level Security 表達，建議選能一次提供 Google 登入、Postgres + RLS、檔案儲存的 BaaS，例如 Supabase 或 Firebase。

- **前端**：任一現代框架 + PWA 插件（Service Worker、manifest）；離線佇列用 IndexedDB。
- **拖曳排序**：用既有的拖拉套件，不要自己寫觸控事件。排序值用整數間隔（1000、2000、3000）插入時取中間值，避免每次重排整欄。
- **連結預覽 / 圖片轉存**：唯一需要自寫的伺服器端邏輯，做成一支 serverless function：驗證 URL → DNS 解析檢查私網 → 抓取（限時限量）→ 解析 OG → 下載圖片 → 壓縮 → 存 bucket → 回傳。
- **行程查詢**：一個專案的整份行程一次撈回（60 天 × 20 筆 = 1200 列上限，可接受），前端依 date / section / slot 分組。不要每天打一次 API。
- **國家清單**：靜態 JSON（ISO 3166-1 + 中文名 + emoji 旗），不需後端。
- **標籤配色**：`hash(name) % 8` 對應固定色票。
- **排除**：即時同步、訊息推播、後台管理介面（v1 沒有管理員角色，資料維護直接操作資料庫）。

---

## 9. 未來版本 (P2) 候選

- 行程匯出成一頁式 PDF 或圖片，離線發給沒帳號的同行者
- 公開唯讀行程連結
- 交通時間自動估算（接 Google Directions）
- 地圖總覽：把當天行程畫在地圖上看順不順路
- Web Share Target：從 IG / Maps 的「分享」直接進 App 新增（Android 支援，iOS 不支援）
- 花費紀錄與統計
- 專案封存 / 複製上一趟的行程為範本
- 標籤跨專案的個人標籤庫
- 轉移專案擁有者
- 多國家專案
- 即時同步（多人同時編行程）
- 餐食候選的「決定吃這間」標記（目前用完成勾選代替）

---

## 10. 已確認決策

### 10.1 第一輪（2026-09-10）

| # | 問題 | 決議 | 反映在 |
|---|---|---|---|
| Q1 | 地點要不要也有「已去過」勾選？ | **加**，P1 | F-36、Item.visited |
| Q2 | 共同分頁的項目，是否任何成員都可刪除？ | 任何成員皆可刪除 | 已被 Q8 取代 |
| Q3 | 是否允許成員（非擁有者）邀請其他人？ | 允許 | F-06 |
| Q4 | 專案需要起迄日期嗎？ | **改為必填**（因 Q9） | F-03 |
| Q5 | 離線是否也要能新增購物項目？ | v1 不做，只切換狀態 | F-33 |
| Q6 | 圖片每項目上限 5 張 | 5 張 | F-27 |
| Q7 | 建立專案時自動預設地區？ | 不預設，使用者自建 | F-05 |

### 10.2 第二輪・行程頁（2026-09-14）

| # | 問題 | 決議 | 反映在 |
|---|---|---|---|
| Q8 | 行程頁與共同分頁的關係 | **取代**。行程全隊共用一份，共同分頁移除 | §3.2、F-09、F-37 |
| Q9 | 行程的「天」怎麼表示 | 綁實際日期，依專案起訖日自動產生 | F-03、F-37、F-46 |
| Q10 | 從清單放進行程是引用還是複製 | **引用連動**，行程裡可再加當天備註 | F-41、§4.2 |
| Q11 | 餐食備選與早中晚時段的關係 | **分兩區**，上方行程、下方餐食，互不干涉 | F-37、F-42 |
| Q12 | 交通方法怎麼記 | 交通就是一個行程項目，類型 = 交通 | F-39 |
| Q13 | 有時間的項目要不要提醒 | 只做視覺標註，不發通知 | F-40、§1.2 |
| Q14 | 行程與清單一起出還是分階段 | **v1 一起做** | 全文 |

### 10.3 PM 在本輪自行決定的事項

以下是寫需求時發現、未經討論但必須有答案的問題。若不同意請提出。

| # | 問題 | 決定 | 理由 |
|---|---|---|---|
| D1 | 行程上勾「完成」要不要寫回清單的「已去過」 | 引用**自己的**地點時同步寫回；引用**他人的**地點時只更新行程 | 沒有權限改別人的清單項目。原本承諾的連動在自己的項目上仍然成立 |
| D2 | 清單項目被刪除時，行程項目怎麼辦 | 行程項目保留，斷開引用改為純文字 | 旅途中看的東西不該因為別人整理清單而消失 |
| D3 | 專案日期縮短時，範圍外的行程怎麼辦 | 不刪，放在日期列最後標示「範圍外」 | 任何會造成資料消失的設計在旅遊情境都太危險 |
| D4 | 已完成的行程項目要不要沉底 | **不要**，位置不動 | 行程是時間順序，沉底會看不出走到哪。這點與購物清單相反是刻意的 |
| D5 | 有時間與沒時間的項目在同一時段怎麼排 | 有時間的在前依時間排，沒時間的在後依手動排序 | 有時間的是硬約束，要先看到 |
| D6 | 購物項目能不能進行程 | 不能 | 購物是「路過就買」，綁在時段上沒有意義。要提醒就用購物的預計購買地點 |
| D7 | 餐食候選要不要「決定吃這間」的標記 | v1 不做，用完成勾選代替 | 少一個欄位與一套 UI。列 P2 |
| D8 | 時間怎麼存 | 純 DATE 與純 TIME，不做時區換算 | 在台灣排、在日本看，用 timestamp 會整份差幾小時 |

---

## 11. v2.0 變更清單（給已看過 v1.1 的人）

**新增**
- §5.10 行程，共 11 條需求 F-37 ~ F-47
- 資料表 `ItineraryEntry`、`TripDay`
- 頁面 P-04（行程分頁）、P-08（行程項目編輯）、P-09（從清單選地點）
- 流程 B（排行程）、流程 D（旅途中看行程）
- 非功能需求新增「時區」一條

**修改**
- `Trip.start_date` / `end_date` 由選填改為**必填**
- `Item.owner_user_id` 由 nullable 改為 **NOT NULL**（共同分頁移除）
- §3.2 權限矩陣：共同分頁三條移除，行程兩條加入
- F-09 分頁列：［共同］改為［行程］，預設落點改為行程
- F-11 / F-12：複製目標只剩「我的分頁」，另增「加入行程」入口
- F-15：地點卡片增加「已排入行程」徽章
- F-32 / F-33：離線範圍涵蓋行程，離線可勾選行程完成
- F-36：補上與行程完成勾選的連動規則
- §1.2 非目標：「行程排程」移除，改為「小時刻度時間軸」「行程提醒通知」

**刪除**
- F-25 共同分頁標籤同名合併（共同分頁不存在了）
- §4.2 標籤同名合併規則
- §9 P2 清單中的「行程排程」「地點已去過」

---

## 附錄 A：驗收摘要（v1 完成定義）

**帳號與專案**
- [ ] Google 登入、登出、逾期重登
- [ ] 建立 / 編輯 / 軟刪除專案，國家選擇，起訖日期必填驗證
- [ ] 地區 CRUD 與排序
- [ ] 邀請連結產生 / 分享 / 撤銷 / 接受 / 過期
- [ ] 成員列表、移除、離開，離開者分頁唯讀

**清單**
- [ ] 分頁列（行程 / 我的 / 成員…），他人分頁唯讀且 API 層拒絕寫入
- [ ] 地點與購物項目 CRUD，欄位齊全
- [ ] 貼連結自動帶標題與圖片，含 SSRF 防護
- [ ] 圖片上傳（壓縮、上限）與貼網址轉存
- [ ] 標籤建立 / 改名 / 刪除，個人所有
- [ ] 地區 / 狀態 / 標籤篩選，AND 組合
- [ ] 購買狀態一鍵切換
- [ ] 地點已去過一鍵切換與篩選
- [ ] 複製項目到我的分頁，標籤依名稱對應

**行程**
- [ ] 日期列依專案起訖日產生，自動捲到今天
- [ ] 早 / 中 / 晚三時段，空時段仍顯示
- [ ] 餐食備選區五餐別，一餐多候選
- [ ] 新增行程項目（自由輸入）≤ 3 次點擊
- [ ] 交通類型與交通方式欄位，視覺區隔
- [ ] 有時間的項目標註並優先排序（F-40 的 AC）
- [ ] 從清單多選加入行程，引用連動（改清單照片行程跟著變）
- [ ] 完成勾選，自己的地點同步 visited
- [ ] 每日備註
- [ ] 拖曳排序、跨時段與跨日搬移
- [ ] 專案日期縮短不遺失資料（F-46 的 AC）
- [ ] 刪除被引用的地點，行程項目保留（F-47 的 AC）

**離線與版面**
- [ ] PWA 可安裝，曾開過的專案離線可讀，含完整行程
- [ ] 離線可切換購買狀態、已去過、行程完成，恢復連線自動同步
- [ ] 手機 360 px 版面無橫向捲動
