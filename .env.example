# 複製成 .env.local（本機用），並在 Vercel 的 Settings → Environment Variables 也填一份。
# .env.local 已經在 .gitignore 裡，不會被推上去。
#
# 兩個值都在 Supabase 後台的 Settings → API Keys 拿得到。
#
# VITE_SUPABASE_URL   專案網址，長得像 https://abcdefghijklm.supabase.co
#
# VITE_SUPABASE_ANON_KEY  貼後台那一長串「anon public」金鑰，整串原樣複製。
#
#   常見錯誤：Supabase 有兩種金鑰格式，新的短字串（sb_publishable_ 開頭）
#   和舊的 JWT（eyJ 開頭）。不要把前綴接在 JWT 前面湊成
#   sb_publishable_eyJ...，那不是合法的金鑰，會回 401 Invalid API key。
#   後台顯示哪一種就整串照抄哪一種。
#
#   絕對不要貼 Secret key（sb_secret_...）或 service_role。
#   那兩個會繞過所有 RLS 權限檢查，而 VITE_ 開頭的變數會被打包進瀏覽器，
#   等於把整個資料庫的鑰匙公開掛在網頁上。

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxx.xxxxxxxxxxxx
